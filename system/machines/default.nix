# todo
# current.nix is outside of Git so we might be unable to pass it.
# OTOH darwin-rebuild will use (hostname?) to determine the configuration to use so we can deal with that.
# import (import ./current.nix)
import ./work.nix
